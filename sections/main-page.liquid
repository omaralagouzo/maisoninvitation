<section class="section" style="padding-block-start: clamp(56px, 8vw, 120px)">
  <div class="page-width page-width--narrow">
    <h1 class="h1" style="margin-block-end: 32px">{{ page.title }}</h1>
    <div class="rte lede">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{
  "name": "Page content",
  "tag": "div",
  "settings": []
}
{% endschema %}
